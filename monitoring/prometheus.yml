global:
  scrape_interval: 5s  # How often to scrape targets

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['prometheus:9090']

  - job_name: 'mybasket-app'
    static_configs:
      - targets: ['host.docker.internal:8000']  # Your app running locally
